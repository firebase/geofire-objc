import PackageDescription

let package = Package(
    name: "geofire_objc",
    products: [
        .library(name: "geofire_objc", targets: ["geofire_objc"])
    ],
    targets: [
        .target(
            name: "GeoFire",
            path: "GeoFire",
    ]
)
